%% Application configuration baked into the release (and picked up by
%% `rebar3 shell`). kv_http reads {kv_store, port} at listener start.
[
  {kv_store, [
    {port, 8080}
  ]},

  {kernel, [
    {logger_level, info}
  ]}
].
